---
interface Props {
  title: string;
  subtitle?: string;
}

const { title, subtitle } = Astro.props;
---

<div class="mb-12 fade-in">
  <div class="accent-line mb-5"></div>
  <h2 class="heading-lg mb-3">{title}</h2>
  {subtitle && <p class="text-ash text-base max-w-2xl">{subtitle}</p>}
</div>
