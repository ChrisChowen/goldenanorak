name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Get repo name
        id: repo
        run: echo "name=$(echo $GITHUB_REPOSITORY | cut -d'/' -f2)" >> $GITHUB_OUTPUT

      - name: Build Astro site
        run: npx astro build --config astro.config.mjs
        env:
          ASTRO_BASE: /${{ steps.repo.outputs.name }}/current

      - name: Fix Astro paths for subdirectory deployment
        run: |
          REPO_NAME=${{ steps.repo.outputs.name }}
          BASE_PATH="/${REPO_NAME}/current"

          # Rewrite absolute /images/ paths to include repo prefix
          find dist/ -name '*.html' -exec sed -i "s|src=\"/images/|src=\"/${REPO_NAME}/images/|g" {} +
          find dist/ -name '*.html' -exec sed -i "s|content=\"/images/|content=\"/${REPO_NAME}/images/|g" {} +

          # Rewrite internal navigation links (href="/...") to include base path
          # Use perl for negative lookahead to avoid double-prefixing Astro-generated paths
          find dist/ -name '*.html' -exec perl -pi -e "s|href=\"/(?!${REPO_NAME}/)|href=\"${BASE_PATH}/|g" {} +

      - name: Assemble pages output
        run: |
          mkdir -p _site

          # Current (Astro) site → /current/
          cp -r dist/ _site/current/

          # New (frontend-design) site → /new/
          mkdir -p _site/new
          cp frontend-design/*.html _site/new/
          cp frontend-design/*.css _site/new/
          # Copy actual images (resolve symlink)
          cp -rL frontend-design/images _site/new/images

          # Shared images at site root so /goldenanorak/images/ resolves
          cp -rL public/images _site/images

          # Landing page at root
          cp _site-landing.html _site/index.html

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
